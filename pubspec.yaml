name: safe_text
description: A Flutter package for filtering out bad words from text inputs.
version: 1.0.7
homepage: https://github.com/master-wayne7/safe_text

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

example: main.dart

flutter:
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
funding:
  - https://www.buymeacoffee.com/ronitrameja
